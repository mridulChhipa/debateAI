from django.urls import path
from . import views

app_name = 'sarvam_integration'

urlpatterns = [
    # API endpoints for Sarvam AI integration
    # path('api/chat/', views.chat_completion, name='chat_completion'),
]
